import 'package:flutter/material.dart';

class LyricsDisplay extends StatelessWidget {
  const LyricsDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LyricsDisplay is working'),
      ),
    );
  }
}