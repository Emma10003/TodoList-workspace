import 'package:flutter/material.dart';

class Equalizer extends StatelessWidget {
  const Equalizer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Equalizer is working'),
      ),
    );
  }
}